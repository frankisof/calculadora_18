export function coseno(num1: number, num2: number) {  
  const grados = num1 * (  Math.PI/180);     
  const coseno = Math.cos(grados);
       
        return coseno;
      
      }
    

      
    
